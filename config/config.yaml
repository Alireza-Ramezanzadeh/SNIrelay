# SNIrelay configuration
# ─────────────────────────────────────────────────────────────────────────────

upstream_proxy:
  type: socks5
  host: "127.0.0.1"
  port: 10808

# Listeners: bind address + proto (tls | http). Routing is only via `hosts` below.
listens:
  - name: https
    addr: "0.0.0.0:443"
    proto: tls

  - name: https-alt
    addr: "0.0.0.0:8443"
    proto: tls

  - name: http
    addr: "0.0.0.0:80"
    proto: http

  - name: http-alt
    addr: "0.0.0.0:8080"
    proto: http

# Allowlist + DNS override (sniproxy `table hosts`).
# address: "*" = tunnel to client SNI/Host; or set an IP to override DNS.
# Add { pattern: ".*", address: "*" } at the end to allow every hostname.
hosts:
  - pattern: translate.wordpress.com
    address: 192.0.78.12
  - pattern: eu.taline.ir
    address: 91.107.161.221
  - pattern: bimemosaferati.com
    address: 95.156.253.59
  - pattern: downloads.wordpress.org
    address: 198.143.164.250

connect_timeout_secs: 10
rw_timeout_secs: 300
workers: 0
nofile_limit: 1048576
max_connections: 50000
listen_backlog: 4096
metrics_addr: "0.0.0.0:9090"
metrics_refresh_secs: 2
